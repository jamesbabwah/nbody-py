[project]
name = "nbody"
version = "0.1.0"
description = "Simulation of bodies in space interacting gravitationally."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy==2.4.2",
    "pygame==2.6.1",
]
